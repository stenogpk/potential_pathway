name: potential_pathway
description: Potential Pathway — source-grounded adaptive exam preparation system.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.7.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^13.1.0
  shared_preferences: ^2.5.5
  syncfusion_flutter_pdf: ^34.2.8
  http: ^1.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
