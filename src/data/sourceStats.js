export function sourceStats(sources, courseNodes = [], contentChunks = []) { return { total: sources.length, selected: sources.filter((s) => s.status === "file-selected").length, indexed: sources.filter((s) => s.status === "indexed" || s.status === "active").length, archived: sources.filter((s) => s.status === "archived").length, attached: sources.filter((s) => courseNodes.some((n) => n.sourceRefs?.includes(s.id))).length, chunks: contentChunks.length }; }
