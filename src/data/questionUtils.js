export function filterQuestions(questions, { missionId, topicId = "all", difficulty = "all", sourceId = "all", tag = "all" }) { return questions.filter((q) => (!missionId || q.missionId === missionId) && (topicId === "all" || q.topicId === topicId) && (difficulty === "all" || q.difficulty === difficulty) && (sourceId === "all" || q.sourceRefs?.includes(sourceId)) && (tag === "all" || q.tags?.includes(tag))); }
export function questionFacets(questions) { return { topics: [...new Set(questions.map((q) => q.topicId))], difficulties: [...new Set(questions.map((q) => q.difficulty))], tags: [...new Set(questions.flatMap((q) => q.tags || []))], sources: [...new Set(questions.flatMap((q) => q.sourceRefs || []))] }; }
